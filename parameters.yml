---

preprocessing_parameters:
  SEED: 1234
  MAX_VOCAB_SIZE: 25_000
  BATCH_SIZE: 64
  train_path: data\Train.csv
  test_path: data\Valid.csv
  vectors: glove.6B.100d
  deterministic: True
  tokenizer_language: en_core_web_sm

model_parameters:
  EMBEDDING_DIM: 100
  HIDDEN_DIM: 256
  OUTPUT_DIM: 1
  N_LAYERS: 2
  BIDIRECTIONAL: True
  DROPOUT: 0.5
  N_EPOCHS: 30
  model_dict_name: my_model_dict.pt
  model_name: my_model.pt
